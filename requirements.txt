# Root requirements for the Python jobs under .github/workflows/.
#
# Six jobs run `pip install -r requirements.txt` from the REPOSITORY ROOT:
#   - test.yml                        (CrystalCastle Python Tests)
#   - Python-CI.yml                   (Python CI, 3.9 / 3.10 / 3.11)
#   - crystalcastle-coderabbit-test.yml
#   - CICD_Pipeline.yaml
#   - errorlog-generator.yml
#   - .github/jobs/build-and-deploy
#
# The FastAPI application's own pins live at backend/requirements.txt and are
# installed separately by FastAPI_CI.yaml with `working-directory: backend`.
# The versions below intentionally match that file for the shared packages, so
# the two manifests cannot drift into a conflict.

# --- application runtime (mirrors backend/requirements.txt) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.4
pydantic-settings==2.3.3
apscheduler==3.10.4
python-dotenv==1.2.1  # 1.2.2 requires >=3.10; Python-CI.yml tests on 3.9
aiosqlite==0.20.0

# --- test tooling ---
# Python-CI.yml and crystalcastle-coderabbit-test.yml both invoke pytest but
# never install it, relying on this file to provide it.
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
requests==2.32.3

# --- used by code under scripts/ ---
pyyaml==6.0.2
aiofiles==24.1.0
aiohttp==3.10.9
aioimaplib==1.1.0
psycopg[binary]==3.2.3
pyflakes==3.2.0
