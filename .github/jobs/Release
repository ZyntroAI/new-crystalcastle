name: Release
on:
  push:
    branches: [ ** ]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 18
      - run: npm ci
      - run: npx semantic-release
